import { AngularSplitDirectiveDirective } from './angular-split-directive.directive';

describe('AngularSplitDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AngularSplitDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
